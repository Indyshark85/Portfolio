#lang racket
;===================================================================;
;                             Part 1                                ;
;===================================================================;
;1 - Not Done

(define list-ref
  (lambda (ls n)
    (letrec
      ([nth-cdr (lambda (n)
                  ;; complete the definition
                  )])
      (car (nth-cdr n)))))

;===================================================================;
;                             Part 2                                ;
;===================================================================;

;2 - Not Done
; You need to fix the function
(define lambda-exp?
  (lambda (E)
    (letrec
      ([p
        (lambda (e)
          (match e
            [var #t]
            [(lam param body) (p body)]
            [(app rator rand) (or (p rator) (p rand))]
            [else #f]))])
      (p E))))

;3
;4
;5
;6
;7
;8
;9



;===================================================================;
;                             Part 3                                ;
;===================================================================;

;10
;11
;12
;13
;14
