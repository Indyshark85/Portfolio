import org.junit.jupiter.api.Test;

public class TODOTestP1 {
    @Test
    void testDequeueP() {
        // construct two DequeueP objects with the same elements and confirm they are equal
        // and that their hashcodes are equal

        // construct two DequeueP objects with different elements and confirm they are not equal
    }

    @Test
    void testDequeueA() {
        // construct two DequeueA objects with the same elements and confirm they are equal
        // and that their hashcodes are equal

        // change the capacity of one of the DequeueA objects and confirm they are still equal
        // and that their hashcodes are still equal

        // construct two DequeueA objects with different elements and confirm they are not equal
    }
}
